defaults:
  - _self_
  - tasks: forecasting
  - models: forecasting_cnn #nontrainable_ewma
  - runner: base_runner
  - data: ett_dataset
  - random_seed: basic
  - optimizer: base

sanity_run: True
experiment_name: ett_forecasting

train:
  device: cuda
  epochs: 5
  early_stopping:
    patience: 3

eval:
  init:
    key_val_metric: mse
    key_test_metric: mse

test: {}

study_name: test_pipeline

hydra:
  run:
    dir: ./outputs/${study_name}
  sweep:
    dir: multirun/timemixer_noise_dependence
    subdir: run_${hydra.job.num}
  sweeper:
    params:
      random_seed.seed: 41, 42, 43
