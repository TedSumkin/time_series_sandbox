_target_: sandbox.tasks.forecasting_task.ForecastingTask

config:
  split:
    train: 0.7
    val: 0.15
    test: 0.15

  normalization:
    type: standard

  window:
    context_length: 24
    horizon: 20
